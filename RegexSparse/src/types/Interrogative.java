package types;
//these words are from different types, but arent used much outside of this function, and have much in common as a result
public class Interrogative extends Node {
	public Interrogative(Word val) {
		super(val);
		symbol = 'q';

	}
}
